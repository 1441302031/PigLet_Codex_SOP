# Spec to Executor Prompt

Read `AGENTS.md` and the relevant spec first.

Then:
- restate the scope boundary
- implement the smallest coherent change
- avoid opportunistic refactors
- validate explicitly
- suggest stable write-back only if justified