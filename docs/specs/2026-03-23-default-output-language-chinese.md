# 2026-03-23 默认输出语言改为中文

## 元数据

- 日期: `2026-03-23`
- 负责人: `Codex`
- 任务: `将仓库默认输出内容语言收敛为中文`

## 计划

- 目标: 定义一个范围较小、可执行的后续任务规范，使该仓库在面向用户的默认输出中使用中文。
- 约束:
  - 本次只创建规范，不实现任何功能。
  - 后续实现应优先修改“输出约束层”，而不是全面翻译整个仓库。

1. 将“默认输出使用中文”收敛为一个独立、可审阅的任务切片。
2. 明确后续实现的目标文件层级与边界。
3. 为后续实现定义可复核的验证标准。

## 规范

### 背景

当前仓库同时包含英文与中文内容。用户提出的新要求是“所有输出内容语言使用中文”。为了避免范围失控，后续实现应优先聚焦于会直接影响代理默认输出语言的规则、提示词和技能说明，而不是立即翻译仓库中的所有历史文档。

### 范围

- 范围内: 定义后续实现中需要调整的默认输出约束；优先覆盖根级行为规则、关键提示模板，以及直接影响用户可见输出语言的技能说明。
- 范围外: 本次创建或切换分支；本次翻译整个仓库；本次修改所有历史 spec；本次重写事实文档；本次实现任何功能或执行任何翻译批量作业。

### 非目标

- 不要求将所有现有 Markdown 文档全文翻译为中文。
- 不要求修改与用户输出无关的仓库结构。
- 不要求改变验证、事实写回、最小改动等现有工作流原则。
- 不要求在没有明确收益的情况下新增多个技能或抽象层。

### 目标文件或系统

- `AGENTS.md`
- `docs/templates/`
- `skills/` 中直接约束输出语言的技能文件

### 假设

- “所有输出内容”优先解释为该仓库工作流产出的默认用户可见输出，而非所有历史仓库文本。
- 后续实现可以通过修改少量规则文件与模板文件，达到大部分输出默认中文的效果。

### 完成标准

- 后续实现后，仓库中的默认输出规则明确要求使用中文。
- 后续实现后，关键模板不会再默认生成英文面向用户输出。
- 验证步骤可以明确检查出这些规则与模板是否已经改为中文导向。

## 验证

- 命令: `Test-Path docs\specs\2026-03-23-default-output-language-chinese.md`
  结果: `应返回 True，确认该任务规范文件已创建。`
- 命令: `Select-String -Path docs\specs\2026-03-23-default-output-language-chinese.md -Pattern '^### 非目标$','输出内容语言','不实现任何功能','^## 验证$'`
  结果: `应匹配到非目标、小范围约束、暂不实现和验证章节。`

## 回滚

本次仅新增规范文件，无需行为层回滚。

## 风险与缓解

- 风险: “所有输出内容”被理解为需要立即翻译整个仓库。 -> 缓解: 后续实现限定为先修改输出约束层和关键模板。
- 风险: 后续实现顺手扩大到仓库全面本地化。 -> 缓解: 明确将全面翻译列为范围外事项，单独立项。

## 变更摘要

新增一份小范围的可执行规范，用于后续将仓库默认输出语言收敛为中文。

## 稳定事实写回

- 更新文件: `N/A`
- 新增或确认的事实: `N/A`
